# TODO: #22 Add workflow to run tests on push and pull requests to main

/models        ← Mongoose-modeller (User, Accommodation)
/routes        ← Express-routes
/tests         ← Vitest-tester
Dockerfile     ← För containerisering
.github/
  workflows/   ← GitHub Actions-konfiguration